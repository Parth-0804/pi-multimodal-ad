# Synthetic multimodal fixture

All files in this directory are generated, tiny, and synthetic. They contain
no PHM or Intel dataset content. The two PPM images use text-based pixel data;
`records.json` contains short sensor arrays, timestamps, groups, and scalar
targets. Unit tests package copies into temporary ZIP files when archive
behavior is needed.
