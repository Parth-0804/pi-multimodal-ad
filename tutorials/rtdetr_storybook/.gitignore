# Generated by make_demo_dataset.py / make_raw_demo_dataset.py: real PHM
# image copies + derived .md targets. Per AGENTS.md, raw data must never
# be committed — these are a scratch teaching aid regenerated on demand,
# not a checked-in dataset.
/demo_data/
/raw_demo_data/

# Generated by rtdetr_storybook.py: figures derived from the above.
/outputs/

# example_run/ is a deliberate, deliberately small exception: a curated
# handful of output figures + a text report, committed on purpose as a
# worked example. Everything else derived from PHM photos stays ignored
# above, matching how runs/ and experiments/ handle generated imagery
# elsewhere in this repo.
