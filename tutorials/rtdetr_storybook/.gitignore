# Generated by make_demo_dataset.py / make_raw_demo_dataset.py: real PHM
# image copies + derived .md targets. Per AGENTS.md, raw data must never
# be committed — these are a scratch teaching aid regenerated on demand,
# not a checked-in dataset.
/demo_data/
/raw_demo_data/

# Generated by rtdetr_storybook.py: figures derived from the above.
/outputs/

# example_run/ is a deliberate, small exception to the "never commit images
# derived from PHM photos" rule above: a handful of curated figures from one
# representative image (step0/step1/step2/step4 only, not the full outputs/
# tree, and not every processed image) are committed so the README's claims
# about what this tutorial produces are checkable without re-running it.
# Keep it minimal — do not check in raw_demo_data/, demo_data/, or outputs/
# themselves.
